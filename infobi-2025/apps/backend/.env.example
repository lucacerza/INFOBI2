# Backend Environment Variables

# JWT Secret Key (CAMBIARE IN PRODUZIONE!)
JWT_SECRET_KEY=your-super-secret-jwt-key-change-in-production

# Database Encryption Key (CAMBIARE IN PRODUZIONE!)
# Genera con: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
DB_ENCRYPTION_KEY=your-encryption-key-here

# Database interno (opzionale, default: data/infobi.db)
# DATABASE_PATH=./data/infobi.db

# Server settings
# HOST=0.0.0.0
# PORT=8090
